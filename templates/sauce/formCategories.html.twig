<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}<link rel="stylesheet" href="{{asset('build/css/creationRestaurant.66df981e.css')}}">{% endblock %}
    </head>
    <body>
        {% form_theme form 'bootstrap_4_layout.html.twig' %}
        {% block body %}
         <div class="py-5 text-left bg-light">
    <div class="container">   
            <a class="nav-link mx-auto text-primary" href="/tbsaucemoutard"><br>Gestion des categories</a>
        {{form_start(form)}}
        {{form_widget(form)}}
        <button type="submit" >ok</button>
        {{form_end(form)}}
        {% endblock %}
        {% block javascripts %}{% endblock %}
        </div>
        </div>
    </body>
</html>
{# empty Twig template #}
{# empty Twig template #}
{# empty Twig template #}
